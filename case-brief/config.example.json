{
  "_comment": "Optional. Copy to config.json and edit. Every field has a CLI flag equivalent (run case_brief.py -h). Defaults work for most users.",

  "azure_devops": {
    "org_url": "https://dev.azure.com/yourorg",
    "project": null
  },

  "output_dir": "./case-briefs",
  "open_in_vscode": true
}
