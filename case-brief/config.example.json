{
  "_comment": "Optional. Copy to config.json and edit -- only the Chrome automation profile is configurable here; everything else (Azure DevOps org, CRM ticket field/host, output dir) is fixed in case_brief.py/lib/ado_api.py since this tool only ever talks to one org and one CRM.",

  "chrome": {
    "profile_dir": "./chrome-automation-profile",
    "debug_port": 9222,
    "executable": null
  }
}
