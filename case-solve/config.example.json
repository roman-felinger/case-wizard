{
  "_comment": "Optional. Copy to config.json and edit. run_tests/run_lint/run_build each have a CLI --skip-* flag equivalent (run case_solve.py -h); claude.* is config-only. guide_dir/solves_dir are fixed paths in case_solve.py, not configurable.",

  "run_tests": true,
  "run_lint": true,
  "run_build": true,
  "claude": {
    "model": null,
    "agent": "case-solver",
    "timeout": 900
  }
}
